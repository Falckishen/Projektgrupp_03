package View;

/**
 * Enum for the types of renderables separate from EntityType since it also contains MISC and NUMERAL.
 */
enum ImageTypeEnum {
    MISC,
    PLAYER,
    PLAYERPROJECTILE,
    GRUNT,
    NUMERAL
}
